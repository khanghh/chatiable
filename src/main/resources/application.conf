chatiable {
  mysql {
    dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
    properties = {
      driver = "slick.jdbc.MySQLProfile$"
      url = "mysql://root:123456@localhost:3306/chatiable?autoReconnect=true&useSSL=false&socketTimeout=3000"
    }
    numThreads = 10
  }
}